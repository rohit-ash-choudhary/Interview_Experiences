Array={3,4,5,5,5}

num_present_more_nby2times=1;
for:i to arr.legtnh=1;
arr[i]^num_present_more_nby2times=



def find_number_morethen_nthbytwo(array)
    number=None
    count=0

    for i in array:
        if count=0;
        number=i;
        count=1;
    elif i==number:
    count+=1;
else:
count=-1;




s ="abcdabcbb" ;  //longest sub string length
0,1,2,3,4,5,6,7


def longest_substring(s):
    char_index={a:4,b:8,c:5,d:3} //3
    left=0; //0  , right =0
    max_length=0;
    for right in range(len(s)):
        s[right] in char_index and char_index[s[right]]>=left:
        left=char_index[s[right]]+1
    char_index[s[right]]=right
    max_length=max(max_length,right-left+1)
return max_length;



Employee :
select max(salary) from employee where salary < (select max(salary) from employee where salary {})

select salary from employee order by salary desc limit offset 4;


Order :

delete from order where order_date < SYSDATE-365*10;  //DATE.FORMAT("MM-DD-YYYY");

update order set is_delete="True" where  order_date < SYSDATE-365*10;  //DATE.FORMAT("MM-DD-YYYY");


 REST API AND THEIR METHOD
REST VS SOAP

ANNOTATION IN SPRING

JWT AUTHENTICATION

OOPS CONCPET